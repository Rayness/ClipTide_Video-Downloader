# ClipTide — runtime dependencies
# Устанавливается: python -m pip install -r requirements.txt
#
# Файл переведён в UTF-8 и закреплён по версиям (раньше был UTF-16 без версий
# и содержал транзитивные/несуществующие пакеты: logging, pathlib, configparser,
# а также пакет `ffmpeg`, чей модуль конфликтовал по имени с `ffmpeg-python`).

# --- Ядро загрузчика ---
yt-dlp==2026.8.19
requests==2.34.2

# --- Медиа и конвертер ---
ffmpeg-python==0.2.0
pillow==12.3.0
pypdfium2==5.13.0          # рендер PDF; заменил PyMuPDF (37 МБ -> 4 МБ, BSD вместо AGPL)

# --- Система ---
psutil==7.2.2

# --- Интерфейс ---
# Только QtCore/QtGui/QtWidgets и т.п., без WebEngine.
# Заменил pywebview + pythonnet + bottle: приложение больше не зависит
# от установленного в системе Edge WebView2 Runtime.
PySide6-Essentials==6.11.2
